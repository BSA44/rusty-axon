# Binary size matrix

Auto-generated by `scripts/measure_binary_size.sh` (or `.ps1`) + `scripts/sizes_to_md.py`. Do not edit by hand.

_Generated: 2026-05-07T18:03:56Z_

Artifact in every row is `examples/min_inference` — the smallest realistic inference binary in the crate.  The `release-edge` profile (`Cargo.toml`) sets `lto = "fat"`, `codegen-units = 1`, `panic = "abort"`, `opt-level = "z"`, and `strip = "symbols"`, so rows that target it are stripped at link time without an extra post-step.

| ID | Profile | Features | Target | Strip | Size |
|---:|---------|----------|--------|:-----:|-----:|
| A | `release` | `train,inference,matrixmultiply` | host | no | 284.5 KiB |
| B | `release` | `inference` | host | no | 231.5 KiB |
| C | `release-edge` | `inference` | host | yes | 193.5 KiB |
| D | `release-edge` | `inference,quant-i8` | host | yes | 195.5 KiB |
| E | `release-edge` | `inference` | aarch64 | yes | — |
| F | `release-edge` | `inference,quant-i8` | aarch64 | yes | — |

Empty `Size` cells indicate the combo could not be built in this environment (typically aarch64 rows when neither `cross` nor `cargo-zigbuild` is on PATH).

## Reproducing

```sh
# Linux / macOS:
bash scripts/measure_binary_size.sh

# Windows (PowerShell):
pwsh scripts/measure_binary_size.ps1
```

Each invocation rebuilds every combo, overwrites `binary_sizes.csv` in the repo root, and rewrites this file.
